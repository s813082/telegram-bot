# ════════════════════════════════════════════════════════════════
# Telegram Bot 環境變數設定檔
# ════════════════════════════════════════════════════════════════
# 使用說明：
# 1. 複製此檔案為 .env：cp .env.example .env
# 2. 填入你的實際設定值
# 3. .env 檔案不會被 git 追蹤，請妥善保管
# ════════════════════════════════════════════════════════════════

# ── 必要設定 ────────────────────────────────────────────────────

# Telegram Bot Token (從 @BotFather 取得)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# 允許的 Telegram User ID (從 @userinfobot 取得)
# 只有此 ID 的使用者可以使用 bot
ALLOWED_USER_ID=your-telegram-user-id-here

# ── Copilot 設定 ────────────────────────────────────────────────

# Copilot 模型 (預設: gpt-4o)
# 可選: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
COPILOT_MODEL=gpt-4o

# Copilot 回應超時時間 (毫秒, 預設: 180000 = 3 分鐘)
COPILOT_TIMEOUT_MS=180000

# ── 訊息與互動設定 ──────────────────────────────────────────────

# 思考中訊息更新間隔 (毫秒, 預設: 30000 = 30 秒)
THINKING_UPDATE_INTERVAL_MS=30000

# 思考中訊息 emoji (逗號分隔)
THINKING_EMOJIS=🤔,💭,⏳,🔍,💡,🧠,⚙️,🔄

# Telegram 單則訊息最大長度 (預設: 4096)
TELEGRAM_MESSAGE_MAX_LENGTH=4096

# ── 速率限制設定 ────────────────────────────────────────────────

# 每個使用者在時間窗口內的最大訊息數 (預設: 5)
RATE_LIMIT_MAX_MESSAGES=5

# 速率限制時間窗口 (毫秒, 預設: 60000 = 1 分鐘)
RATE_LIMIT_WINDOW_MS=60000

# ── 記憶系統設定 ────────────────────────────────────────────────

# 中期記憶保留天數 (預設: 30)
# 超過此天數的記憶檔案會被自動刪除（重要記憶除外）
MEMORY_RETENTION_DAYS=30

# 載入最近幾天的記憶到 session (預設: 3)
MEMORY_RECENT_DAYS=3

# 記憶體清理排程間隔 (毫秒, 預設: 300000 = 5 分鐘)
MEMORY_CLEANUP_INTERVAL_MS=300000

# ── 記憶分類排程設定 ────────────────────────────────────────────

# 記憶分類排程 cron 表達式
# 測試模式：每 5 分鐘執行一次
SCHEDULER_CRON=*/5 * * * *
# 正式模式：每日凌晨 2:00 執行
# SCHEDULER_CRON=0 2 * * *

# ── 日誌設定 ────────────────────────────────────────────────────

# 日誌級別 (預設: info)
# 可選: error, warn, info, debug
LOG_LEVEL=info

# 日誌檔案路徑 (預設: ./logs)
LOG_PATH=./logs

# ── 可選設定 ────────────────────────────────────────────────────

# Node 環境 (預設: development)
# 可選: development, production
NODE_ENV=development

# HTTP 代理 (若需要)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# ── Email 功能 (未實作) ──────────────────────────────────────────

# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# ADMIN_EMAIL=admin@example.com

# ── Vision API (未實作) ──────────────────────────────────────────

# Google Vision API Key (若使用 OCR)
# VISION_API_KEY=your-google-vision-api-key
